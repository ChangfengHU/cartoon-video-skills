# Scene and action production packs

0.6.1 extends the owned studio-character-workflow with independent naming, actual scene images, composited samples, named pose frames, source hashes and Fleet scene links. Imported designer bytes are unchanged. Evidence validation does not assert aesthetic approval.

Validated: production-pack tests, studio mirrors/import locks. Fleet scene UI is maintained in the separate Fleet repository. Installing the package does not install private assets or automatically update a running client. Roll back by reinstalling the preceding pinned release; preserve private asset revisions.
